import React from 'react';
import { BlogRouter } from '../components/blog/BlogRouter';

// This would be the main blog page (/blog)
export const BlogPage: React.FC = () => {
  return <BlogRouter />;
};